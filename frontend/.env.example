# FastAPI backend URL (dev proxy — not needed in production if frontend/backend share a domain)
FASTAPI_URL="http://localhost:8000"

# Public API base URL (empty = same origin via Next.js rewrite proxy; set for direct calls in prod)
NEXT_PUBLIC_API_URL=""

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"
